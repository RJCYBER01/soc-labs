const button=document.querySelector('.menu');const links=document.querySelector('.nav-links');if(button&&links){button.addEventListener('click',()=>{const open=links.classList.toggle('open');button.setAttribute('aria-expanded',String(open))});links.addEventListener('click',()=>{links.classList.remove('open');button.setAttribute('aria-expanded','false')})}const certificateFiles={'/certifications/computer-hardware-basics/':'/certificates/computer-hardware-basics.pdf','/certifications/operating-systems-basics/':'/certificates/operating-systems-basics.pdf'};const certificateFile=certificateFiles[window.location.pathname];if(certificateFile){const actions=document.querySelector('.credential .actions');if(actions){const view=document.createElement('a');view.className='btn';view.href=certificateFile;view.target='_blank';view.rel='noopener';view.textContent='View original certificate';actions.prepend(view)}}
